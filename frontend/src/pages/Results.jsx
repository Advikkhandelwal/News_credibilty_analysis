import React from 'react';
import { useLocation, Link } from 'react-router-dom';
import { motion } from 'framer-motion';
import { ShieldAlert, ShieldCheck, Download, AlertTriangle, FileText, Bot, AlertCircle } from 'lucide-react';
import { RadialBarChart, RadialBar, PolarAngleAxis } from 'recharts';

export default function Results() {
  const location = useLocation();
  const result = location.state?.result;

  if (!result) {
    return (
      <div className="flex flex-col items-center justify-center min-h-[50vh]">
        <div className="w-16 h-16 bg-zinc-100 rounded-full flex items-center justify-center mb-6">
          <AlertTriangle className="w-8 h-8 text-zinc-500" />
        </div>
        <h2 className="text-xl font-semibold mb-2 text-zinc-900">No Analysis Data Found</h2>
        <p className="text-zinc-500 mb-6 text-sm">Please submit an article for analysis first.</p>
        <Link to="/analyze" className="bg-zinc-900 hover:bg-zinc-800 text-white px-6 py-3 rounded-lg transition-colors font-medium text-sm">
          Go to Analyzer
        </Link>
      </div>
    );
  }

  const { ml_prediction, agent_report } = result;
  
  const isCredible = ml_prediction.label === 'Credible';
  const isSuspicious = ml_prediction.label === 'Suspicious';
  
  const labelColor = isCredible ? 'text-emerald-700' : isSuspicious ? 'text-amber-700' : 'text-red-700';
  const bgColor = isCredible ? 'bg-emerald-50' : isSuspicious ? 'bg-amber-50' : 'bg-red-50';
  const borderColor = isCredible ? 'border-emerald-200' : isSuspicious ? 'border-amber-200' : 'border-red-200';
  const chartFill = isCredible ? '#047857' : isSuspicious ? '#b45309' : '#b91c1c';
  
  const chartData = [
    { name: 'Confidence', value: ml_prediction.confidence_percentage, fill: chartFill }
  ];

  return (
    <motion.div 
      initial={{ opacity: 0, y: 15 }}
      animate={{ opacity: 1, y: 0 }}
      className="container mx-auto px-4 py-12 max-w-5xl"
    >
      <div className="flex flex-col md:flex-row items-center justify-between mb-10 gap-6 border-b border-zinc-200 pb-8">
        <div>
          <h1 className="text-3xl font-bold flex items-center gap-3 text-zinc-900 mb-2">
            <FileText className="text-zinc-400 w-8 h-8" />
            Credibility Report
          </h1>
          <p className="text-zinc-500 text-base">Detailed analysis generated by ML & Llama 3</p>
        </div>
        <button 
          onClick={() => window.print()}
          className="flex items-center gap-2 bg-white border border-zinc-200 hover:bg-zinc-50 text-zinc-700 px-5 py-2.5 rounded-lg font-medium transition-all text-sm subtle-shadow"
        >
          <Download size={16} /> Export PDF
        </button>
      </div>

      <div className="grid lg:grid-cols-3 gap-6 mb-6">
        {/* ML Verdict Card */}
        <motion.div 
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.1 }}
          className={`lg:col-span-2 p-8 rounded-2xl border ${borderColor} ${bgColor} flex flex-col justify-center`}
        >
          <div className="flex flex-col md:flex-row items-start md:items-center gap-6">
            <div className={`p-4 rounded-xl bg-white border ${borderColor} ${labelColor}`}>
              {isCredible ? <ShieldCheck size={48} /> : isSuspicious ? <AlertCircle size={48} /> : <ShieldAlert size={48} />}
            </div>
            <div>
              <div className="flex items-center gap-2 mb-2">
                <span className="px-2.5 py-1 bg-white rounded-md text-xs font-semibold uppercase tracking-wide text-zinc-600 border border-zinc-200">ML Prediction</span>
                <span className="px-2.5 py-1 bg-white rounded-md text-xs font-semibold uppercase tracking-wide text-zinc-500 border border-zinc-200">{ml_prediction.model_used}</span>
              </div>
              <h2 className={`text-4xl md:text-5xl font-bold mb-3 tracking-tight ${labelColor}`}>
                {ml_prediction.label}
              </h2>
              <p className="text-zinc-700 leading-relaxed max-w-xl text-base">
                Based on historical training data, this article demonstrates linguistic patterns typical of <strong className="font-semibold">{ml_prediction.label.toLowerCase()}</strong> content.
              </p>
            </div>
          </div>
        </motion.div>

        {/* Confidence Card */}
        <motion.div 
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.2 }}
          className="bg-white border border-zinc-200 rounded-2xl p-8 flex flex-col items-center justify-center relative subtle-shadow"
        >
          <p className="text-zinc-500 uppercase tracking-widest text-xs font-semibold w-full text-center mb-2">Confidence Score</p>
          <div className="relative flex items-center justify-center">
            <RadialBarChart 
              width={200} 
              height={200} 
              innerRadius="75%" 
              outerRadius="100%" 
              data={chartData} 
              startAngle={90} 
              endAngle={-270}
            >
              <PolarAngleAxis type="number" domain={[0, 100]} angleAxisId={0} tick={false} />
              <RadialBar minAngle={15} background={{ fill: '#f4f4f5' }} clockWise={true} dataKey="value" cornerRadius={12} />
            </RadialBarChart>
            <div className="absolute flex flex-col items-center justify-center">
              <span className="text-3xl font-bold text-zinc-900">{ml_prediction.confidence_percentage.toFixed(1)}<span className="text-xl text-zinc-400">%</span></span>
            </div>
          </div>
        </motion.div>
      </div>

      <div className="grid lg:grid-cols-5 gap-6 mb-12">
        {/* Agent Report */}
        <motion.div 
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.3 }}
          className="lg:col-span-3 bg-white border border-zinc-200 rounded-2xl p-8 subtle-shadow"
        >
          <div className="flex items-center gap-3 mb-6 pb-6 border-b border-zinc-100">
            <div className="p-2.5 bg-zinc-50 rounded-lg border border-zinc-200">
              <Bot className="text-zinc-700 w-6 h-6" />
            </div>
            <div>
              <h3 className="text-xl font-bold text-zinc-900">Agentic Verification</h3>
              <p className="text-zinc-500 text-xs font-medium uppercase tracking-wider">Powered by Llama 3</p>
            </div>
          </div>
          
          <div className="bg-zinc-50 p-6 rounded-xl border border-zinc-200 mb-8 relative">
            <div className="absolute left-0 top-0 bottom-0 w-1 bg-zinc-300 rounded-l-xl" />
            <h4 className="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-2">Final Verdict</h4>
            <p className="text-zinc-800 leading-relaxed text-base font-medium">
              "{agent_report.final_verdict}"
            </p>
          </div>
          
          <div className="space-y-8">
            <div>
              <h4 className="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-2">Article Summary</h4>
              <p className="text-zinc-600 leading-relaxed text-sm">{agent_report.article_summary}</p>
            </div>
            
            <div className="flex flex-wrap items-center gap-2 bg-white p-3 rounded-lg border border-zinc-200 inline-flex">
              <span className="text-xs font-bold text-zinc-500 uppercase tracking-widest">Reasoning Confidence:</span>
              <span className={`px-3 py-1 rounded-md text-xs font-bold border ${
                agent_report.confidence_level === 'High' ? 'bg-emerald-50 text-emerald-700 border-emerald-200' :
                agent_report.confidence_level === 'Medium' ? 'bg-amber-50 text-amber-700 border-amber-200' :
                'bg-red-50 text-red-700 border-red-200'
              }`}>{agent_report.confidence_level}</span>
            </div>
          </div>
        </motion.div>

        {/* Indicators & Keywords */}
        <motion.div 
          initial={{ opacity: 0 }}
          animate={{ opacity: 1 }}
          transition={{ delay: 0.4 }}
          className="lg:col-span-2 space-y-6"
        >
          <div className="bg-white border border-zinc-200 rounded-2xl p-8 subtle-shadow">
            <h3 className="text-base font-bold mb-4 text-emerald-700 flex items-center gap-2">
              <ShieldCheck className="w-4 h-4" /> Credibility Indicators
            </h3>
            {agent_report.credibility_indicators.length > 0 ? (
              <ul className="space-y-2">
                {agent_report.credibility_indicators.map((ind, i) => (
                  <li key={i} className="flex items-start gap-2 text-sm text-zinc-600">
                    <span className="text-emerald-500 mt-0.5 shrink-0">•</span> 
                    <span className="leading-relaxed">{ind}</span>
                  </li>
                ))}
              </ul>
            ) : (
              <p className="text-zinc-400 text-sm italic">No significant credibility indicators found.</p>
            )}
          </div>
          
          <div className="bg-white border border-zinc-200 rounded-2xl p-8 subtle-shadow">
            <h3 className="text-base font-bold mb-4 text-red-700 flex items-center gap-2">
              <ShieldAlert className="w-4 h-4" /> Risk Factors
            </h3>
            {agent_report.risk_factors.length > 0 ? (
              <ul className="space-y-2">
                {agent_report.risk_factors.map((risk, i) => (
                  <li key={i} className="flex items-start gap-2 text-sm text-zinc-600">
                    <span className="text-red-500 mt-0.5 shrink-0">•</span> 
                    <span className="leading-relaxed">{risk}</span>
                  </li>
                ))}
              </ul>
            ) : (
              <p className="text-zinc-400 text-sm italic">No significant risk factors identified.</p>
            )}
          </div>
          
          <div className="bg-white border border-zinc-200 rounded-2xl p-8 subtle-shadow">
            <h3 className="text-xs font-bold text-zinc-500 uppercase tracking-widest mb-4">ML Flagged Keywords</h3>
            <div className="flex flex-wrap gap-2">
              {ml_prediction.important_keywords.map((kw, i) => (
                <span key={i} className="bg-zinc-50 text-zinc-700 px-3 py-1 rounded-md text-xs font-medium border border-zinc-200">
                  {kw}
                </span>
              ))}
            </div>
          </div>
        </motion.div>
      </div>
      
      <div className="text-center text-xs text-zinc-400 max-w-3xl mx-auto px-4">
        <p className="border-t border-zinc-200 pt-8">{agent_report.ethical_disclaimer}</p>
      </div>
    </motion.div>
  );
}